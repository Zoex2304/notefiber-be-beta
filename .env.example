# Environment Variables for NoteFiber Backend
# Save this as .env file in the root directory

# Database Configuration
DB_CONNECTION_STRING=postgresql://username:password@localhost:5432/database_name

# Google Gemini API Configuration
GOOGLE_GEMINI_API_KEY=your_gemini_api_key_here

# Message Queue Configuration (used for embedding)
EMBED_NOTE_CONTENT_TOPIC_NAME=embed_note_content

# Server Configuration (optional)
PORT=3000
ENV=development

# Database Schema Details:
# Make sure your PostgreSQL database has the following extensions:
# - pgvector (for embedding similarity search)
# 
# Example PostgreSQL setup commands:
# CREATE EXTENSION IF NOT EXISTS vector;
# CREATE DATABASE your_database_name;

# Environment Setup Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual configuration
# 3. Make sure the database connection string follows this format:
#    postgresql://username:password@host:port/database_name
# 4. Obtain your Gemini API key from Google Cloud Console
# 5. Set the EMBED_NOTE_CONTENT_TOPIC_NAME for the message queue

# Required Environment Variables for Chatbot Feature:
# 1. DB_CONNECTION_STRING
#    - Required for database connection
#    - Must include valid PostgreSQL connection details
#
# 2. GOOGLE_GEMINI_API_KEY
#    - Required for:
#      a. Chat completion (gemini_chatbot.go)
#      b. Text embeddings (gemini_embedding.go)
#      c. RAG decisions (chatbot_service.go)
#
# 3. EMBED_NOTE_CONTENT_TOPIC_NAME
#    - Required for embedding queue management
#    - Used in publisher and consumer services

# Security Notes:
# - Never commit the .env file with real values
# - Keep your Gemini API key secure
# - Use strong database passwords
# - Consider using different database users for different environments

# Example Complete Configuration:
# DB_CONNECTION_STRING=postgresql://notefiber_user:secure_password@localhost:5432/notefiber_db
# GOOGLE_GEMINI_API_KEY=AIza...your_key_here
# EMBED_NOTE_CONTENT_TOPIC_NAME=embed_note_content
# PORT=3000
# ENV=development